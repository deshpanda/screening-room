# Everything personal stays local. Only ciphertext (data/vault.enc) is committed.
export/
export*/
*.zip
tools/.tmdb-cache.json
tools/.imdb-ratings.tsv
config.local*
.env*

# macOS / editor
.DS_Store
*.log
node_modules/
